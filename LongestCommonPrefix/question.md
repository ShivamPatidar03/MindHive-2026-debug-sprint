# Q1 — Common Prefix Analyzer

You are given an array of strings.

Find the longest common prefix among all the strings.

If there is no common prefix, return an empty string.

---

## Example

Input:

["flower", "flow", "flight"]


Output:

fl

---

## ⚠️ Rules

- Do NOT rewrite the whole program.
- Debug loop, indexing, and substring logic carefully.
